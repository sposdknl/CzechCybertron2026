---
- name: Import Zabbix templates
  ansible.builtin.include_role:
    name: community.zabbix.zabbix_agent
  vars:
    zabbix_agent_version: 7.0
    zabbix_agent2: true
    zabbix_agent_hostname: "ansible"
    zabbix_agent_listenip: "{{ ansible_host }}"
    zabbix_agent_server: 10.15.15.100
    zabbix_agent_serveractive: 10.15.15.100
    zabbix_agent_host_state: present
...